import 'package:flutter/material.dart';

class BillingsPage extends StatelessWidget {
  const BillingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Billings page")));
  }
}
